# 2026-09-24

## 评估外部技能 mickpletcher/AI-Skills · astrophotography-processing

- 目标：判断该外部 Claude Skill 是否对本仓库 deepsky-skill 有优化价值。
- 方法：拉取仓库完整文件树与全部内容（SKILL.md / 8 个 references / 2 个脚本 / 模板），与本地 6 个 Skill 逐项对照，并用 grep 核实覆盖缺口。
- 结论：借鉴价值低—中，不引入本体。该技能是纯文档型 router，约 40 KB，无测试/版本/门禁；能力广度重叠但深度低本项目一个量级。
- 已确认的真缺口：**全仓库无太阳观测安全护栏**（唯一能力型缺口）。
- 可借鉴：①太阳安全护栏 ②"最多 3 问 + 显式标注假设" ③显式操作模式标注 ④执行型 Skill 输出模板补"不要做什么"小节。
- 已确认的覆盖情况（用于后续避免重复调研）：
  - 银河/彗星/行星/宽场 → `deep-sky-processor/references/special_targets.md` 已深度覆盖。
  - APP/DSS/Lightroom/GIMP/Affinity → 有 `external_tools.md`、`后期软件对比.md`、`photoshop_workflow.md`，但相对偏薄。
  - `when_to_use` frontmatter 不可直接引入：`scripts/validate_repository.py` 的 `ALLOWED_FRONTMATTER_KEYS` 仅允许 name/description/license/allowed-tools/metadata。
  - 产出报告：`.workbuddy-ai/analysis/2026-09-24-astrophotography-processing-skill-review.md`

## 用 siril-moon-stacking 实跑 ~/SeeStar/Moon（53 张 ORF）

- 输入：`/Users/mz/SeeStar/Moon`（53 张 Panasonic `.ORF`，无 FITS）；新工作目录 `/Users/mz/SeeStar/Moon_work_20260924`（隔离，未触碰原目录）。
- 运行环境：Python venv `/Users/mz/.workbuddy-ai/binaries/python/envs/default`（本次新装 numpy/astropy/scipy/pillow/opencv-python-headless/scikit-image）；Siril 1.4.4 CLI 位于 `/Applications/Siril.app/Contents/MacOS/siril-cli`。
- 结果：Otsu 选帧保留 24/53（45.3%，与"凸月 40~50%"决策表吻合），参考帧 #23；产出 `moon_natural.jpg` / `moon_mineral.jpg`（deep-cine）/ `moon_mineral_natural.jpg` / `moon_natural.tif`。
- **踩坑（已修）**：`requirements-dev.txt` 只有 pytest+PyYAML，缺全部运行时依赖 → 已新增 `siril-moon-stacking/requirements.txt`，并在 SKILL.md Step 1、RELEASING.md、CHANGELOG `[Unreleased]` 同步说明。
- **环境注意**：沙箱会拦截 Siril 写 `~/Library/Application Support/org.siril.Siril/siril/config.1.4.ini`，日志出现 `Could not save the settings`——**无害**，Siril 仍正常执行；不要据此判断步骤失败，应以工作目录产物为准。
- **注意**：`scripts/moon_stack.py` 不生成 `logs/` 以外的中间态说明；`all` 子命令可一键跑完全流程。
- 产出可视化报告 `~/SeeStar/Moon_work_20260924/moon_processing_report.html`（自包含单页，内嵌 base64 成片 + 手写 SVG 图表，生成脚本 `/tmp/build_moon_report.py`）。
- **光学参数反推**：由月盘实测直径（眩光抑制 RANSAC 拟合 R=1047.6px → D≈2095px）× 3.73µm = 7.82mm，结合月球视直径 0.49°~0.558°，反推**有效焦距 ≈ 864mm（802~914mm）**。而本次用的是技能默认 `--aperture 80 --focal 400`（f/5），艾里斑尺度偏小约 2 倍。**下次处理前应向用户确认真实口径与焦距**，或从月盘直径反推后传参。
